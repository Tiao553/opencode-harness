---
description: Second-opinion quality assessment using external judge runtime with OpenRouter
agent: dev.judge-agent
subtask: true
---

Execute the global `/review:judge` command.

Before acting, read these files:
- `~/.config/opencode/skills/review/SKILL.md`
- `~/.config/opencode/skills/review/commands/judge.md`

Treat those files as the canonical workflow for this command.
User arguments: `$ARGUMENTS`

If the command references supporting artifacts, load them lazily from `~/.config/opencode/`.
