---
description: Generate a comprehensive project status report — active features,
  recent decisions, agent recommendations, and health assessment
---

Execute the global `/core:status` command.

Before acting, read these files:
- `~/.config/opencode/skills/core-commands/SKILL.md`
- `~/.config/opencode/skills/core-commands/commands/status.md`

Treat those files as the canonical workflow for this command.
User arguments: `$ARGUMENTS`

If the command references supporting artifacts, load them lazily from `~/.config/opencode/`.
