---
description: Generate a visual HTML implementation plan — detailed feature
  specification with state machines, code snippets, and edge cases
---

Execute the global `/visual:generate-visual-plan` command.

Before acting, read these files:
- `~/.config/opencode/skills/visual-explainer/SKILL.md`
- `~/.config/opencode/skills/visual-explainer/commands/generate-visual-plan.md`

Treat those files as the canonical workflow for this command.
User arguments: `$ARGUMENTS`

If the command references supporting artifacts, load them lazily from `~/.config/opencode/`.
